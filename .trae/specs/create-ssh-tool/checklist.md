* [ ] Flutter 项目初始化完成（作为后端宿主）。

* [ ] Vue 3 项目初始化完成（作为前端 UI）。

* [ ] Flutter 应用启动后，Shelf 服务器正常监听端口。

* [ ] Flutter 后端可通过 dartssh2 连接 SSH。

* [ ] WebSocket 服务正常，前端 xterm.js 可连接并交互。

* [ ] 终端显示远程 Shell 提示符，命令执行正常。

* [ ] SFTP 功能正常，文件列表可加载。

* [ ] 前端文件管理器支持上传、下载。

* [ ] 文本编辑器可读写远程文件。

* [ ] 监控仪表盘显示 CPU/RAM 实时数据。

* [ ] UI 风格仿 MacOS。

* [ ] Docker 管理预留接口存在。

